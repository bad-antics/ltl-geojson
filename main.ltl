// ltl-geojson/main.ltl
//
// GeoJSON RFC 7946 parser / validator / pretty-printer.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-geojson v0.1.0")
    io::println("GeoJSON RFC 7946 parser / validator / pretty-printer.")
    0
}
